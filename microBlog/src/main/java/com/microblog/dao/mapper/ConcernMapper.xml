<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.microblog.dao.mapper.ConcernMapper">
	<!-- 查询用户的粉丝 -->
	<select id="getUsersFans">
	select uid,nickname from users where uid in
	(select f_uid from concern c 
	inner join users u on c.b_uid=u.uid 
	where c.b_uid=#{uid})
	</select>
	
	<!-- 查询用户的粉丝总数 -->
	<select id="getUserFansCount">
	select count(1) from concern where b_uid=#{uid}
	</select>
	
	<!-- 添加关注 -->
	<insert id="addUserConcern">
		
	</insert>
</mapper>